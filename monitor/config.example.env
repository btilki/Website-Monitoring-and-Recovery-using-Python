# Copy to ./monitor/.env and fill in real values
# Example:
# cp monitor/config.example.env monitor/.env
# Edit monitor/.env and set your SMTP credentials and target URL.

# Website to monitor (can be app service or external URL)
TARGET_URL=http://app:8000/health

# Name of the container to restart (matches docker-compose service container_name)
CONTAINER_NAME=sample_app

# How often to check (seconds)
CHECK_INTERVAL=15

# Number of consecutive failures before triggering restart & alert
FAILURE_THRESHOLD=3

# Number of attempts to restart the container before optionally rebooting host
RETRY_RESTART=2

# If set to "true", attempt host reboot when restarts fail
REBOOT_ON_FAILURE=false

# Email settings (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
ALERT_FROM=monitor@example.com
ALERT_TO=you@example.com